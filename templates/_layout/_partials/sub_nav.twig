{# -------------------------------------------------
   Module: Sub Nav
------------------------------------------------- #}

{#
   -------------------------------------------------
#}


<section class="m-sub_nav | flex flex-wrap md:flex-nowrap  divide-x divide-gray-400 | border-t border-gray-400">
	{% for child in children %}
	<a href="{{ child.url }}" class="w-full md:flex-auto text-center | py-5 px-6 | uppercase font-bold {% if entry.url == child.url %}bg-gray-400 text-black{% else %}bg-white text-red-400 hover:bg-red-400 hover:text-white{% endif %}">{{ child.title }}</a>
	{% endfor %}
</section>