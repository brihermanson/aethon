 {# -------------------------------------------------
   Module: Card Menu Parent
------------------------------------------------- #}

{# Macros #}

{% set image 		= 	card.c_image.one() %}
{% set parent		=	card.menuParent.one() %}
{% set entries 		= 	parent.children.all() %}

{#
   -------------------------------------------------
#}

<div class="l-item w-full sm:w-1/2 lg:w-1/3 | | mb-10 lg:mb-0">
	<div class="pb-1/2 relative text-white">
		<img class="absolute top-0 left-0 w-full h-full object-cover" src="{{ image.url }}" />
		<h3 class="e-header_five absolute w-full bottom-0 left-0 pb-6 px-8">{{ parent.title }}</h3>
	</div>
	<div class="bg-white px-8 py-8">
		{% for entry in entries %}
		<div>
			<a class="e-link | inline-block text-5 text-red-400 my-2" href="{{ entry.url }}">{{ entry.title }}</a>
		</div>
		{% endfor %}
		
				{% for entry in entries %}
		<div>
			<a class="e-link | inline-block text-5 text-red-400 my-2" href="{{ entry.url }}">{{ entry.title }}</a>
		</div>
		{% endfor %}
	</div>
</div>
