{# -------------------------------------------------
   Component: Basic Paragraph
------------------------------------------------- #}

{# Macros #}

{% set headline 	= 	tab.plainHeadline %}
{% set stat			= 	tab.stat %}

{#
   -------------------------------------------------
#}

<div class="l-item | flex max-w-4xl mx-auto my-4 py-8 px-10 items-center | bg-white border-l-8 border-red-400">
	<div class="w-full md:w-1/2">
		<h3 class="font-proxima text-black text-6 font-light">{{ headline }}</h3>
	</div>
	<div class="w-full md:w-1/2 text-right">
		<h4 class="font-minion text-red-400 text-6 font-normal">{{ stat }}</h4>
	</div>
</div>